## this is a maybe.
  ## if I do this, I need to bring in the three separate GPLS algorithms, and one that has a switch between them
    ## which will require quite a bit of testing, probably.
gpls <- function(){

}
